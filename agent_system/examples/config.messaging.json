{
  "provider": "openai",
  "model": "gpt-4o-mini",
  "vision_provider": "openai",
  "vision_model": "gpt-4o-mini",
  "workspace": "./workspace",
  "max_steps": 40,
  "skills": [
    "files",
    "pdf",
    "docparse",
    "office",
    "messaging",
    "present"
  ],

  "_комментарий_messaging": "Сами секреты (пароли/токены) НЕ храним здесь — только в .env/переменных окружения: SMTP_HOST/SMTP_USER/SMTP_PASSWORD, IMAP_HOST/IMAP_USER/IMAP_PASSWORD, TELEGRAM_BOT_TOKEN, MAX_BOT_TOKEN. Здесь — только не-секретные параметры протокола.",
  "messaging": {
    "email": {
      "smtp_port": 587,
      "smtp_use_ssl": false,
      "smtp_starttls": true,
      "from_addr": "bot@example.com",
      "imap_port": 993,
      "imap_use_ssl": true
    },
    "telegram": {
      "rate_limit": 1.0
    },
    "max": {
      "rate_limit": 0.6
    },
    "_комментарий_confirm_sends": "true (по умолчанию) — перед реальной отправкой письма/сообщения агент спрашивает подтверждения оператора (как run_command для опасных команд). Для автономного прогона без оператора поставьте false осознанно.",
    "confirm_sends": true
  },

  "_комментарий_сценарий": "Пример потока «фото/текст → анализ → отчёт»: пользователь присылает файл через email_read/telegram_get_updates/max_get_updates -> агент сохраняет вложение в workspace -> pdf_classify/pdf_extract или doc_classify/doc_extract режут на структуру -> extract_entities кладёт сущности в онтологию -> pptx_create/pptx_add_slide/pptx_add_table_slide собирают отчёт -> telegram_send_file/max_send_message/email_send отправляют его обратно."
}
