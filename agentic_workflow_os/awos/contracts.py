"""Общие маркеры контрактов среды. Нижний слой: ни от чего не зависит.

ЗАЧЕМ ОТДЕЛЬНЫЙ МОДУЛЬ. Роли (`awos/roles/`) формируют запросы к модели
по шаблонам среды, а офлайн-провайдер `StubLLM` (`awos/llm/`) должен
понимать, ЧТО у него спросили, чтобы ответить в нужной форме. Прямая
зависимость между ними невозможна: `roles` уже импортирует `llm` —
получилось бы кольцо. Поэтому маркеры вынесены в модуль без единого
импорта из проекта, и оба слоя зависят от него, а не друг от друга.

ПОЧЕМУ РОЛЬ ОПОЗНАЁТСЯ ПО МАРКЕРУ, А НЕ ПО СИСТЕМНОМУ ПРОМПТУ.
Системный промпт пишет пользователь, и слово «Критик» законно
встречается в промпте Исполнителя («твою работу проверит Критик»).
Опознание ролей по тексту промпта на этом ломается — причём молча:
Исполнитель начинает отвечать JSON-вердиктом, а среда принимает его за
результат работы. Маркер же принадлежит шаблону СРЕДЫ, и его наличие
однозначно: так может спросить только сама платформа.
"""
from __future__ import annotations

#: Есть в каждом запросе к Критику (см. roles/critic.py: CRITIC_TEMPLATE).
CRITIC_MARKER = "Разбери результат и верни ТОЛЬКО JSON-объект"

#: Есть в каждом запросе к Контролёру (roles/supervisor.py).
SUPERVISOR_MARKER = "accept — результат можно использовать"

__all__ = ["CRITIC_MARKER", "SUPERVISOR_MARKER"]
