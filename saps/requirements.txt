# САПС требует PostgreSQL — это принцип Database-First (ТЗ п.2.3).
psycopg[binary]>=3.1

# --- чтение PDF (справочники АП, ТЗ в PDF) ---
# PDF — бинарный формат со сжатием потоков и своими кодировками шрифтов;
# стандартной библиотекой его не разобрать. Достаточно ЛЮБОЙ из двух:
pymupdf>=1.23          # быстрее, лучше держит сложную вёрстку
# pypdf>=4.0           # чистый Python, ставится где угодно (запасной вариант)
#
# Без них Word/Excel и все агенты работают как обычно — откажет только
# загрузка PDF, и то с понятной инструкцией.

# --- только для тестов ---
# embedded PostgreSQL, чтобы набор проверок запускался на машине без
# развёрнутого сервера. В продакшене не нужен.
pgserver>=0.1.4
