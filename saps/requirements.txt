# САПС требует PostgreSQL — это принцип Database-First (ТЗ п.2.3).
psycopg[binary]>=3.1

# Только для тестов: embedded PostgreSQL, чтобы набор проверок
# запускался на машине без развёрнутого сервера. В продакшене не нужен.
pgserver>=0.1.4
