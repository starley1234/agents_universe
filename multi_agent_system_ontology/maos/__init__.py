"""MAOS — Multi-Agent Ontology System.

Независимое приложение (см. README.md в корне пакета). Не импортирует
и не зависит от ../agent_system в рантайме — общие архитектурные идеи и
стиль (докстрины "почему", ленивые импорты опциональных зависимостей,
секреты только в окружении, тесты на реальной инфраструктуре)
позаимствованы осознанно, но код независим и версионируется отдельно.

Ключевое отличие от agent_system: там один агент с инструментами и
SQLite/Postgres как один из вариантов хранения; здесь — НЕСКОЛЬКО
именованных агентов-личностей (со своим голосом, аватаром, LLM),
которые живут как записи в обязательной PostgreSQL+pgvector базе, и
оркестратор выбирает между ними по векторному сходству описаний
(semantic router), а не спрашивает LLM "кто должен ответить".
"""
from __future__ import annotations

__version__ = "0.1.0"
